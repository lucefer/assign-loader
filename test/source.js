let nn dd cc=4, ce,a b c=5
console.log(1)
try{
  while(cursor !== -1) {
    cursor = getCodeFrag()
    if(cursor !== -1) {
      let frag = getFrag()
      if(frag && frag.code.trim() !== '') {
        result.push(frag)
      }
    }
  }
  return generator(result)
}
catch(ex){
  console.error("assign-loader编译错误:",ex)
}
return src
let n,m e f g=10
